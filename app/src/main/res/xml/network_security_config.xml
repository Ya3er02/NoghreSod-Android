<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 
    Security configuration for API calls
    - Production: HTTPS only with certificate pinning
    - Debug: Allow localhost for development
    
    Reference: https://developer.android.com/training/articles/security-config
    -->
    
    <!-- Production Configuration -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificates -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- API Certificate Pinning (Optional - for production) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.noghresod.ir</domain>
        
        <!-- Certificate Pinning: Force specific certificates only -->
        <pin-set expiration="2027-12-31">
            <!-- 
            TODO: Generate certificate pins using:
            openssl s_client -connect api.noghresod.ir:443 < /dev/null | \
            openssl x509 -noout -pubkey | \
            openssl pkey -pubin -outform der | \
            openssl dgst -sha256 -binary | \
            openssl enc -base64
            
            Example (NOT real):
            <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>
            -->
        </pin-set>
    </domain-config>
    
    <!-- Development Configuration - Allow HTTP for testing -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain> <!-- Android emulator gateway -->
        <domain includeSubdomains="true">dev-api.noghresod.ir</domain>
    </domain-config>
</network-security-config>
