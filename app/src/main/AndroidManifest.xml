<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.noghre.sod">

    <!-- ============== INTERNET & NETWORK PERMISSIONS ============== -->
    <!-- Required for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Required to check network connectivity status -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Optional: For WiFi connectivity info -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- ============== CAMERA PERMISSIONS (for future features) ============== -->
    <!-- For product image capture -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- For file access from camera -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- ============== LOCATION PERMISSIONS (optional) ============== -->
    <!-- For delivery location tracking -->
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:node="remove" />
    
    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION"
        tools:node="remove" />

    <!-- ============== ANDROID 12+ PERMISSIONS ============== -->
    <!-- For schedule exact alarms (if needed) -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:node="remove" />

    <!-- ============== FEATURE DECLARATIONS ============== -->
    <!-- Not required but allows filtering on Google Play -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <!-- ============== APPLICATION CONFIG ============== -->
    <application
        android:name="com.noghre.sod.NoghreSodApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:debuggable="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="false"
        android:requestLegacyExternalStorage="false"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoghreSod"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <!-- ============== ACTIVITIES ============== -->
        <activity
            android:name="com.noghre.sod.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Deep linking support -->
            <intent-filter
                android:autoVerify="true"
                android:label="@string/app_name">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="noghresod.com"
                    android:pathPrefix="/product"
                    android:scheme="https" />
            </intent-filter>
        </activity>

        <!-- ============== SERVICES ============== -->
        <!-- FCM Push Notifications (if using Firebase) -->
        <service
            android:name="com.noghre.sod.service.PushNotificationService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- ============== CONTENT PROVIDERS ============== -->
        <!-- For sharing files securely -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- ============== META-DATA ============== -->
        <!-- Firebase configuration -->
        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="barcode" />

        <!-- RenderScript support level -->
        <meta-data
            android:name="com.android.graphics.hwui.drop_shadow_cache_size"
            android:value="6" />

    </application>

</manifest>
